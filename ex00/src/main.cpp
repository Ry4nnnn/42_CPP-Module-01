#include "Zombie.hpp"

int main(void)
{
	// Zombie	*foo = newZombie("Foo");
	// foo->announce();

	// Zombie	*bar = newZombie("Bar");
	// bar->announce();

	randomChump("Zomb");

	// delete bar;
	// delete foo;
	// system("leaks Zombie");
	return (0);
}